post:
  x-mojo-to: 'Cluster::Auth::LDAP#disable'
  operationId: cluster_auth_ldap_disable
  tags:
    - auth
    - cluster
  responses:
    204:
      description: LDAP authentication disabled successfully.
    401:
      $ref: '#/components/responses/Unauthorized'
