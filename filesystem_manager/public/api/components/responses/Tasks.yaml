description: Tasks list
content:
  application/json:
    schema:
      allOf:
        - $ref: '#/components/schemas/DefaultResponse'
        - type: object
          properties:
            entity:
              type: array
              items:
                $ref: '#/components/schemas/Task'
